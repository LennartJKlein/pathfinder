✓ 0.  Een programma schrijven met een 2-dimensionaal grid

  1.  Data structure voor gates op het grid maken + inladen (met eigen simpele data)
    ✓  1.1 ID van tuple inladen in set_gate()
    ✓  1.2 Comment header maken in gates.csv die niet wordt ingeladen
       1.3 Verwijzen in comments naar maker van de board-class
       1.4 Aanroepen van calculatePath in pathfinder.py
       1.5 Load gates.csv into a dict in pathfinder (maak class aan die gates.csv leest)
    ✓  1.6 update labels to start at 0
       	✓  1.6.1 Align vertical labels to the left.

✓ 2.  Inladen van (simpele) netlist in een data structure

✓ 3.  Route berekenen tussen punt A en B op een grid (obv simpele netlist)

  4.  Efficiëntste route berekenen tussen punt A en punt B

  5.  Obstakels ontwijken op een grid in het berekenen van een route

  6.  Gemaakte route onthouden in een data structure

  7.  2 routes berekenen: obstakels ontwijken + vorige route ontwijken.

  8. Zet het bestand om in een programma.
